**嵌入式学习平台**

&#x20;

项目功能详细说明文档

*Qianyi Embedded Learning Platform · React + TypeScript · Vite*

 

# 一、项目概述

嵌入式学习平台是一个专为嵌入式开发学习者设计的交互式在线练习系统，涵盖 C 语言核心语法、数据结构、单片机原理及嵌入式系统知识。整个项目采用 React + TypeScript + Vite 技术栈构建，具有高度互动性，区别于传统选择题平台，强调「输入式答题」——用户必须亲手输入答案，而非点击选项，从而强化真实编码记忆。

 

**前端框架**

React 18 + TypeScript，全组件化架构，大量使用 memo / useCallback / useMemo 性能优化

**构建工具**

Vite，支持懒加载（lazy import），大组件按需加载

**UI 图标**

Lucide React 图标库 + 自定义 SVG 图标系统

**后端服务**

本地 GCC 编译服务（localhost:3001），支持实时编译运行 C 代码，心跳检测连接状态

**AI 能力**

集成 DeepSeek / 自定义大模型 API，提供智能辅导与提示功能

**进度存储**

通过 progressApi 服务持久化保存用户学习进度

 

 

# 二、页面与视图结构

整个应用是单页面应用（SPA），通过 view 状态字符串切换不同视图，无路由跳转，共包含以下 8 个主要视图：

 

## 2.1 首页（HomeView）

首页是用户进入平台的第一个界面，分为以下几个区域：

•        英雄区（Hero Section）：展示平台口号「输入式答题·拒绝无脑选择」，平台名称及覆盖领域。

•        统计卡片区：横排展示 5 个核心数据卡片，分别为「已完成题数」「答对题数」「正确率」「待攻克（答错）题数」「已收藏题数」，每个卡片可点击跳转到对应功能。

•        快速操作区：当存在错题时显示「复习错题」按钮；当存在收藏题时显示「查看收藏」按钮。

•        章节筛选与列表：可按「全部 / 真题 / 测试」三种模式筛选章节，每张章节卡片显示章节图标、名称、类型标签、描述、进度条（答对数/总题数）及箭头指示，点击进入该章节训练。

•        四大训练模式介绍区：以图文卡片形式介绍代码填空、输出预测、Debug 找错、代码排序、代码跟打五种模式，其中「代码跟打」可直接点击跳转。

 

## 2.2 训练视图（TrainingView）

进入某章节后的核心练习界面，包含以下功能：

•        知识讲解（LessonView）：在进入题目前先展示知识点讲解，支持上一页/下一页翻页，内容支持代码块渲染（\`\`\`c 代码块）和加粗文字，并展示关键要点列表。用户可点击「开始练习」直接跳过讲解。

•        进度条：显示当前章节练习进度（第 N 题 / 共 M 题），百分比渐变填充。

•        题目导航：始终可见的「上一题 / 下一题」按钮，支持自由切换，已答题目保存答案状态。

•        答题区域（QuestionView）：根据题型动态渲染不同交互形式（详见第三章）。

•        答对自动跳题：答对后 1.5 秒自动进入下一题。

•        章节完成提示：完成最后一题后弹出「章节完成」提示，显示奖杯图标并提供返回首页按钮。

 

## 2.3 统计视图（StatsView）

•        顶部展示详细统计面板（StatsPanel）：学习天数、连续答题天数、平均每日题数等深度数据。

•        成就面板（AchievementsPanel）：展示所有可解锁成就，已解锁成就高亮显示。

•        总体进度区：以大数字展示已完成、答对、答错题数，并附正确率进度条。

•        章节详情表格：每个章节一行，显示答对数/总题数及百分比进度条。

•        危险操作区：「重置所有进度」按钮，需二次确认（confirm 弹窗），不可撤销。

 

## 2.4 错题复习（ReviewView）

•        自动筛选所有答错过的题目，按顺序呈现。

•        答对后自动从错题本中移除，并进入下一题（1.5 秒延迟）。

•        全部复习完成后自动返回首页，显示「没有错题」时有成功提示。

•        支持收藏/取消收藏操作，支持下一题跳过。

 

## 2.5 收藏视图（BookmarkedView）

•        展示所有已收藏的题目，支持前后翻页，附进度条。

•        可在答题过程中随时取消收藏，收藏列表实时更新。

•        列表为空时展示「暂无收藏」空状态页面。

 

## 2.6 项目实战（ProjectsListView + ProjectView）

•        项目列表页：展示所有实战项目，每个项目显示名称、难度等级（中等/进阶/专家）、描述、片段数量、代码行数、技术标签。

•        项目详情页（ProjectView）：进入项目后以分片方式阅读完整项目代码，每个代码片段（fragment）独立展示，支持前后片段导航，全屏布局（max-width:none）以便更好展示大量代码。

 

## 2.7 互动实验室（LabView）

实验室包含 6 个独立交互模块，通过标签切换，详见第四章。

 

## 2.8 代码跟打（CodeTypingPractice）

独立的代码打字练习视图，详见第五章。

 

 

# 三、题型系统（7 种题型）

平台设计了 7 种不同题型，每种题型都有独立的交互逻辑和反馈机制，统一由 QuestionView 组件负责渲染与状态管理。

 

## 3.1 代码填空（fill / multi-fill）

题目展示带空白的代码片段，用户在行内 input 输入框中直接输入缺失的关键字或语法。

•        空格占位符为 \_\_\_①②③，每个对应一个 input 输入框，内嵌于代码块中。

•        支持多个空格（multi-fill）依次填写，Tab 键切换到下一个输入框，Enter 键提交。

•        输入框宽度随输入内容动态扩展（最小 60px）。

•        支持 caseSensitive 配置，部分题目大小写不敏感。

•        支持多个正确答案（answer 为数组），任意一个正确即得分。

•        自动规范化输入：中文符号（，。；（）【】｛｝）自动转换为英文对应符号。

•        答错后输入框显示红色样式，并在旁边展示正确答案。

 

## 3.2 输出预测（output）

完整展示一段 C 代码，用户需预测程序的输出结果并填写在多行文本框中。

•        使用 textarea，支持 Tab 键插入制表符（模拟代码编辑器行为）。

•        提交时对答案做规范化处理：去除行尾空格、统一换行符（\r\n → \n），再进行精确匹配。

•        答错后同时展示用户答案与正确答案，使用 code 标签区分显示。

 

## 3.3 Debug 找错（debug）

展示一段含有错误的代码，用户输入有错误的行号。

•        代码使用带行号高亮的 CodeBlock 组件展示。

•        答对后自动高亮显示错误行（highlightLine 属性）。

•        展示正确修复方案（bugFix 代码块）。

 

## 3.4 代码排序（order）

提供打乱顺序的代码行，用户依次点击选择，按正确顺序组装完整程序。

•        底部展示「可选代码行」列表，每行可点击选中；选中后移入上方「组装区」。

•        支持「撤销」（取消最后一步）和「重置」（清空所有已选）操作。

•        提交后对每一行进行逐行对比，正确行显示绿色，错误行显示红色。

•        答错后展示正确顺序；答对时若有 correctOutput，额外展示程序运行结果。

 

## 3.5 选择题（choice / multi-choice）

展示题干和 A/B/C/D 等选项卡片，用户点击选项即提交答案（单选），无需额外确认。

•        选项卡片支持三种状态样式：未选 / 选中（蓝色）/ 正确（绿色）/ 错误（红色）。

•        multi-choice 支持多个正确答案，答案以逗号分隔比对。

•        答案提交后立即显示对错结果。

 

## 3.6 代码追踪（trace）

展示代码及追踪问题，用户以文本形式输入变量变化的追踪结果（如 a=10, b=20）。

 

## 3.7 概念理解（concept）/ 代码补全（code-complete）/ 阅读理解（reading）

这三种扩展题型支持选项列表或文本输入两种回答形式，根据题目数据动态渲染。

 

 

# 四、答题界面通用功能

所有题型在 QuestionView 中共享以下通用功能：

 

## 4.1 题目信息头部

•        题型 Badge：彩色标签区分代码填空 / 输出预测 / Debug找错 / 代码排序 / 选择题。

•        难度星级：1-3 星，黄色实心星表示当前难度。

•        收藏按钮：右上角书签图标，点击收藏/取消，图标实时切换状态。

 

## 4.2 提示系统

•        「显示提示」按钮：点击展开题目特定提示（question.hint）或通用提示（按题型生成）。

•        通用提示涵盖：填空题的关键字输入技巧、输出题的变量追踪方法、Debug 题的常见错误检查顺序、排序题的程序结构骨架思路。

 

## 4.3 参考侧边栏（ReferenceSidebar）

答题时左侧（大屏幕）或上方（小屏幕）显示参考信息面板，包含：

•        当前题型说明（如「代码填空（手打关键字）」）。

•        考查焦点：当前章节名 + 题型特定考查点（如拼写记忆、执行顺序等）。

•        常见错误提醒：根据题目代码内容自动生成（如代码含 scanf 则提示「别忘 & 符号」）。

•        词汇表（Vocabulary）：从题目文本中自动提取 C 语言关键字，合并题目自定义词汇，展示每个词的含义解释，帮助理解。

 

## 4.4 答题结果反馈

•        答对：显示绿色对勾 + 「完全正确！」文字 + 当前连续答对数（StreakDisplay）。

•        答错：显示红色叉号 + 「还需要再想想」文字。

•        详细解析：展示 question.explanation，支持多行文本，帮助用户理解错误原因。

 

## 4.5 答题后深度分析

答题提交后依次展示以下深度内容模块：

•        增强版内存可视化（EnhancedMemoryViz）：根据代码自动生成执行步骤，分步展示变量值变化（支持二进制、十六进制、十进制三种格式），用户可逐步前进/后退观察内存状态。

•        代码分析器（CodeAnalyzer）：对每行代码做逐行解析说明（lineAnalysis），配合内存单元格可视化（memoryCells），点击行号高亮对应行，展示该行的内存变化说明。

•        知识点标签：显示 question.knowledgePoints 数组中的所有相关知识点。

•        常见错误提示：显示 question.commonMistakes 数组，列举该题常见的思维误区。

•        相关概念链接：显示 question.relatedConcepts 数组，点击可跳转扩展学习。

•        深入理解面板（DeepUnderstandingPanel）：根据代码内容智能识别涉及的概念（指针、数组、循环、结构体等），展开折叠式面板，每个概念有标题、解释和关键词高亮。

 

## 4.6 声音反馈

集成 useSoundEffects hook，答对播放正确音效，答错播放错误音效，按钮点击有点击音效（均可在无声模式下静音）。

 

## 4.7 键盘快捷键

集成 useKeyboardShortcuts hook，支持以下快捷键：

•        Enter：提交答案（非输出题）。

•        Ctrl+Enter / Shift+Enter：进入下一题（答题后）。

•        H 键：切换提示显示。

•        方向键 ← →：翻题（前后题导航）。

 

 

# 五、互动实验室（6 个模块）

实验室是平台的特色功能区，通过可视化交互帮助用户建立对底层概念的直观理解，每个模块均可独立使用。

 

## 5.1 指针与内存（PointerSandbox）

•        可视化展示变量地址、指针指向关系、解引用操作。

•        支持多级指针（指针的指针）的链式可视化。

•        展示危险操作（野指针、空指针解引用）的后果演示。

•        可交互修改变量值，实时观察内存变化。

 

## 5.2 函数栈帧（StackFrameViz）

•        可视化函数调用时栈的入栈/出栈过程。

•        展示每个栈帧的局部变量、参数、返回地址。

•        支持递归函数调用栈的层层展开/收缩动画。

•        帮助理解局部变量的生命周期及作用域。

 

## 5.3 COW 写时复制机制（COWMemoryViz）

这是一个多步骤动画演示组件，共 5 个步骤：

•        步骤 1「初始状态」：父进程拥有 3 页物理内存（数据页、代码页）。

•        步骤 2「fork() 执行」：fork 时只复制页表，不复制物理页，父子共享页面并标记只读。

•        步骤 3「子进程读取」：子进程读操作直接访问共享物理页，无需复制。

•        步骤 4「子进程写入」：子进程写入时触发写保护中断，内核复制物理页，子进程获得独立副本。

•        步骤 5「最终状态」：父子进程各有独立的数据页，代码页继续共享。

•        支持自动播放（isPlaying 状态），点击页面卡片可高亮显示选中页的详细信息。

 

## 5.4 位运算开关箱（BitSwitchGame）

•        8 个物理开关按钮，每个对应 1 个二进制位（bit 7 到 bit 0）。

•        每个开关底部显示位权（128/64/32/16/8/4/2/1）。

•        点击开关实时更新：二进制字符串、十进制值、十六进制值（0x00-0xFF）。

•        直观建立二进制与十进制/十六进制的转换感知。

 

## 5.5 内存对齐拖拽游戏（StructPackerGame）

•        提供 char(1B) / short(2B) / int(4B) / long(8B) 四种数据类型卡片。

•        32 字节内存格子，用户拖拽字段放置到内存中。

•        自动检查对齐规则：char 可放任意位置，short 需 2 字节对齐，int 需 4 字节对齐，long 需 8 字节对齐。

•        对齐不满足时拒绝放置，防止错误操作。

•        支持撤销（undo）和清空（clear）操作，历史记录保留在 history 数组中。

 

## 5.6 在线代码运行（CodeRunner）

与本地 GCC 后端服务（localhost:3001）通信的在线 C 代码编译运行环境：

•        内置代码编辑器：支持 C 语言关键字/类型/常量/字符串/注释/预处理指令全语法高亮，高亮使用 tokenize 函数自实现词法分析。

•        预设代码库：内置大量预设代码片段（Hello World、指针基础、链表、冒泡排序等），可通过下拉菜单一键加载。

•        编译运行：点击「运行」按钮发送代码到后端 GCC 编译并执行，返回标准输出和错误信息。

•        运行结果展示：支持成功输出（绿色）、编译错误（红色）的不同样式展示，保留空白格式。

•        快捷键支持：Ctrl+Enter 快速运行。

•        复制代码：一键复制编辑器中的代码到剪贴板。

•        全屏模式：支持展开为全屏模式编辑，再收缩回普通视图。

•        后端离线提示：当 GCC 后端未连接时显示友好提示，引导用户启动本地服务。

 

 

# 六、代码跟打练习（CodeTypingPractice）

独立的打字训练模块，帮助用户通过反复打字强化代码语法记忆：

 

•        预设代码库：内置数十个 C 语言代码片段（来自 IMPORTED\_PRESETS），涵盖入门到高级难度。

•        实时字符匹配：用户打字时逐字符高亮比对，正确字符显示绿色，当前位置光标闪烁，错误字符显示红色背景。

•        精准统计：实时计算打字速度（WPM，每分钟字数）、准确率（%）、用时（秒）。

•        完成检测：打完整段代码后自动弹出完成提示，展示最终成绩。

•        难度筛选：可按「入门 / 基础 / 中级 / 高级」筛选预设代码。

•        代码选择下拉：可通过下拉菜单切换不同预设代码片段。

•        重新开始：随时重置当前练习，恢复到未开始状态。

•        暂停/继续：支持暂停计时，暂停时隐藏代码（防止偷看）。

 

 

# 七、AI 辅导系统

平台集成了可配置的 AI 辅导功能，支持接入多个大语言模型提供商：

 

## 7.1 AI 辅导面板（AITutor）

•        题目提交后可点击「AI 辅导」按钮展开 AI 解析面板。

•        自动请求 AI 对当前题目做详细解释（getAIExplanation），返回 explanation 和 hints 数组。

•        「获取提示」按钮：分 3 个层级逐步获取更详细的提示（hintLevel 1-3），避免直接剧透答案。

•        支持刷新重试（RefreshCw 按钮）。

•        错误状态处理：API 调用失败时显示友好错误提示。

 

## 7.2 AI 配置面板（AIConfigPanel）

完整的 AI 服务商配置界面：

•        支持多个 AI 提供商配置（默认包含 DeepSeek）。

•        每个提供商可配置：API Key、Base URL、默认模型、启用/禁用状态。

•        「测试连接」功能：发送测试请求验证 API Key 是否有效，展示成功/失败状态。

•        「获取可用模型」功能：自动拉取当前提供商支持的模型列表，供用户选择。

•        配置持久化：通过 aiConfigService 保存到本地存储。

 

## 7.3 AI 提示词配置（AIPromptConfigPanel）

•        允许用户自定义 AI 的系统提示词（System Prompt），调整 AI 辅导的风格和侧重点。

•        支持重置为默认提示词。

 

 

# 八、进度管理与成就系统

 

## 8.1 进度数据结构（Progress）

用户进度以 Progress 对象存储，包含以下字段：

**completed**

已作答的题目 ID 数组（无论对错）

**correct**

答对的题目 ID 数组

**wrong**

答错的题目 ID 数组（错题本）

**attempts**

每道题的尝试次数 Map（题目ID → 次数）

**bookmarked**

已收藏的题目 ID 数组

**analyzed**

已进行深度分析的题目 ID 数组（预留字段）

 

## 8.2 用户统计（useUserStats）

•        学习天数统计。

•        连续答题天数（currentStreak），答对时实时更新并在结果区展示。

•        分章节正确率统计。

•        平均每日答题量。

 

## 8.3 成就系统（AchievementsPanel）

•        预设多个可解锁成就（如「初次答题」「连续 5 题正确」「完成某章节」等）。

•        成就面板以网格布局展示所有成就，已解锁成就高亮，未解锁灰色显示。

 

 

# 九、全局功能

 

## 9.1 顶部导航栏（Header）

•        Logo + 平台名称：点击返回首页。

•        GCC 连接状态：绿色/红色小圆点 + 文字，实时显示后端服务是否在线（每 5 秒心跳检测）。

•        导航按钮：首页、项目、统计、实验室、跟打练习，当前页面高亮。

•        主题切换按钮：太阳/月亮图标，切换深色/浅色模式。

•        正确率 Badge：统计按钮旁展示总体正确率百分比（仅有答题记录时显示）。

 

## 9.2 深色/浅色主题

•        通过 isDarkMode 状态全局控制，所有组件均支持双模式样式。

•        主题偏好持久化到 localStorage（key: 'theme'）。

•        默认为深色模式。

 

## 9.3 进度持久化

•        进度数据通过 progressApi 服务自动保存。

•        应用启动时自动加载已保存的进度。

•        任何进度变更（答题、收藏、书签）都会触发自动保存。

 

## 9.4 代码高亮（SyntaxHighlighter / HighlightedLine）

•        全平台代码块统一使用自定义语法高亮组件。

•        支持 C 语言关键字、类型、常量、字符串、注释、预处理指令的分色高亮。

•        CodeBlock 组件内置一键复制按钮，复制后按钮文字短暂变为「已复制」。

•        支持高亮指定行（highlightLine 属性），用于 Debug 题的错误行标注。

 

## 9.5 性能优化

•        大量使用 React.memo 防止子组件不必要的重渲染。

•        useCallback / useMemo 缓存事件处理函数和计算结果。

•        CodeRunner / CodeTypingPractice / EnhancedMemoryViz 三个大组件使用 React.lazy + Suspense 懒加载，首屏不加载。

•        样式常量（textStyles）提取为对象，避免字符串拼接重复计算。

 

 

# 十、数据文件与服务层

 

**src/data/lessons.ts**

章节（Chapter）、知识点讲解（Lesson）、题目（Question）的所有数据定义，包含 questions 数组和 chapters 数组

**src/data/projects.ts**

项目实战数据（Project），每个项目含多个 Fragment（代码片段），包括代码内容、说明和标签

**src/data/vocabulary.ts**

词汇表数据及自动推导逻辑（deriveVocabularyFromText），从代码文本中自动识别 C 语言关键词并生成解释

**src/data/importedPresets-full.ts**

跟打练习预设代码库（IMPORTED\_PRESETS），包含大量 C 代码练习素材

**src/services/progressApi.ts**

学习进度的加载（loadProgress）和保存（saveProgress）服务，封装本地存储逻辑

**src/services/aiTutorService.ts**

AI 辅导 API 调用服务（getAIExplanation / getAIHint），处理请求和错误

**src/services/aiConfigService.ts**

AI 配置管理（loadAIConfig / saveAIConfig / testAIProvider / getSupportedModels）

**src/utils/generateStepsFromCode.ts**

根据 C 代码字符串自动生成执行步骤（StepState\[]），用于内存可视化的逐步演示

**src/hooks/useInteractions.ts**

useSoundEffects（音效）和 useKeyboardShortcuts（键盘快捷键）两个自定义 Hook

**src/components/StatsAndAchievements.tsx**

useUserStats Hook + StatsPanel + AchievementsPanel + StreakDisplay 组件

**src/components/ReferenceSidebar.tsx**

答题参考侧边栏组件，展示模式说明、词汇表、常见错误

**src/components/SyntaxHighlighter.tsx**

C 语言语法高亮组件 HighlightedLine

**src/components/ProjectView\.tsx**

项目实战详情视图组件

**src/components/PointerSandbox.tsx**

指针内存可视化交互组件

**src/components/StackFrameViz.tsx**

函数栈帧可视化组件

**src/components/PointerMemoryViz.tsx**

指针内存辅助可视化组件

**src/components/ThemeToggle.tsx**

主题切换按钮组件

**src/components/LiquidNav.tsx**

液态动画导航组件（实验性）

**src/components/LiquidTabs.tsx**

液态动画标签组件（实验性）

**src/components/PageTransition.tsx**

页面切换过渡动画组件

 

 

# 十一、功能总览速查表

以下表格汇总平台所有主要功能模块，方便快速检索：

**功能分类**

**功能名称**

**核心特点**

答题系统

**7种题型**

填空/输出/Debug/排序/选择/追踪/概念，强制输入式

答题系统

**参考侧边栏**

题型说明+考查焦点+常见错误+自动词汇表

答题系统

**提示系统**

按题型生成通用提示+题目专属提示，可收起

答题系统

**深度分析**

内存步进可视化+代码逐行解析+知识点+深入理解

答题系统

**收藏与书签**

随时收藏/取消，独立收藏浏览视图

答题系统

**键盘快捷键**

Enter提交、方向键翻题、H键提示

答题系统

**声音反馈**

答对/答错/点击三种音效

学习视图

**知识讲解**

章节前置学习内容，支持代码块+关键点列表

学习视图

**进度可视化**

进度条+完成数/总数实时显示

学习视图

**错题复习**

自动筛选错题，答对自动移除错题本

互动实验室

**指针内存**

指针链可视化，支持多级指针+危险操作演示

互动实验室

**函数栈帧**

调用栈入栈/出栈动画，递归可视化

互动实验室

**COW机制**

5步骤动画演示fork与写时复制全过程

互动实验室

**位运算开关**

8位开关，实时三进制系统转换显示

互动实验室

**内存对齐游戏**

拖拽字段，对齐规则验证，支持撤销

互动实验室

**在线编译运行**

连接本地GCC，完整语法高亮，预设代码库

AI辅导

**自动解析**

答题后AI生成题目解释和提示列表

AI辅导

**分级提示**

3级渐进式提示，避免直接给答案

AI辅导

**多服务商配置**

支持DeepSeek等多个API，可自定义提示词

进度统计

**成就系统**

多个可解锁成就，解锁状态持久化

进度统计

**详细统计**

连续天数/学习天数/章节正确率/每日答题量

全局功能

**深色/浅色主题**

全平台双模式支持，偏好本地持久化

全局功能

**代码跟打**

字符级比对，WPM+准确率实时统计

全局功能

**项目实战**

多项目代码分片阅读，难度分级，标签筛选

 

# 十二、主题切换规范与常见问题修复

## 12.1 问题描述

在切换白天/夜间模式时，发现部分组件的小背景仍然保持深色，导致在浅色模式下显示不协调。以下是经过全面代码排查后的完整问题清单。

> **⚠️ 重要**：LLM 在实现此项目时，必须严格按照本章规范处理所有颜色和样式，否则会出现主题切换不一致的问题。

### 12.1.1 CSS 全局样式缺少浅色适配（`src/index.css`）

以下 CSS 类只定义了深色模式样式，**缺少 `body:not(.dark)` 浅色模式适配**：

#### (a) 排序题型相关

| CSS 类名 | 行号 | 深色样式 | 浅色模式下的问题 |
|----------|------|----------|-----------------|
| `.order-result-area` | ~781 | `background: rgba(255,255,255,0.02)` `border: 1px solid rgba(255,255,255,0.08)` | 背景和边框在浅色模式下几乎不可见 |
| `.order-line-btn` | ~788 | `background: linear-gradient(135deg, #0d1117, #161b22)` `color: #e6edf3` | 按钮背景始终为深黑色，文字为浅白色 |

#### (b) 可视化组件相关（`.viz-*` 系列）

| CSS 类名 | 行号 | 深色样式 | 浅色模式下的问题 |
|----------|------|----------|-----------------|
| `.viz-container` | ~948 | `background: rgba(255,255,255,0.02)` `border: 1px solid rgba(255,255,255,0.06)` | 容器背景和边框不可见 |
| `.viz-memory-cell` | ~960 | `background: linear-gradient(135deg, #1e293b, #0f172a)` | 记忆单元始终深色 |
| `.viz-array-cell` | ~983 | `background: linear-gradient(135deg, #1e293b, #0f172a)` | 数组单元始终深色 |
| `.viz-ptr-box` | ~1014 | `background: linear-gradient(135deg, #1e293b, #0f172a)` | 指针框始终深色 |
| `.viz-flow-action` | ~1061 | `background: linear-gradient(135deg, #1e293b, #0f172a)` `color: #94a3b8` | 流程动作框始终深色 |
| `.viz-stack-frame` | ~1085 | `background: linear-gradient(135deg, #1e293b, #0f172a)` | 栈帧始终深色 |
| `.viz-table td` | ~1143 | `border-bottom: 1px solid rgba(255,255,255,0.05)` `color: #cbd5e1` | 表格文字在浅色背景上对比度极低 |
| `.viz-execution` | ~1166 | `background: rgba(0,0,0,0.2)` | 执行区域形成突兀暗斑 |
| `.viz-exec-cell` | ~1172 | `background: rgba(255,255,255,0.03)` `border: 1px solid rgba(255,255,255,0.06)` | 背景和边框不可见 |

#### (c) 其他全局样式

| CSS 类名 | 行号 | 深色样式 | 浅色模式下的问题 |
|----------|------|----------|-----------------|
| `::-webkit-scrollbar-track` | ~835 | `background: rgba(255,255,255,0.05)` | 滚动条轨道不可见 |
| `::-webkit-scrollbar-thumb` | ~839 | `background: rgba(255,255,255,0.2)` | 滚动条滑块不可见 |
| `.list-item-hover:hover` | ~1730 | `background: rgba(255,255,255,0.03)` | 列表悬浮效果不可见 |
| `.btn-liquid::after` | ~1602 | `background: rgba(255,255,255,0.15)` | 液态按钮涟漪效果不明显 |
| `.nav-slider` | ~1398 | `box-shadow: inset 0 1px 0 rgba(255,255,255,0.05)` | 导航滑块内阴影不协调 |

#### (d) 已正确适配的样式（参考）

以下样式已有 `body:not(.dark)` 适配，**无需修改**，可作为参考：
- `.glass` / `.glass-hover`（第 449-467 行）✅
- `.code-wrapper` 及其子元素（第 621-636 行）✅
- `.code-block` 及其子元素（第 1288-1318 行）✅
- `.skeleton`（第 475 行）✅

### 12.1.2 实验室组件固定深色背景问题

以下组件在 JSX 中使用了固定的深色 Tailwind 类名，**没有接收 `isDarkMode` 属性进行条件判断**：

| 组件 | 文件路径 | 问题代码 | 出现次数 |
|------|----------|----------|---------|
| BitSwitchGame | `src/components/games/BitSwitchGame.tsx` | `from-slate-900 via-purple-900 to-slate-900` | 1处 |
| StructPackerGame | `src/components/games/StructPackerGame.tsx` | `from-slate-900 via-indigo-900 to-slate-900` | 1处 |
| COWMemoryViz | `src/components/memory/COWMemoryViz.tsx` | `from-slate-900 via-slate-800 to-slate-900` | 1处 |
| StackFrameViz | `src/components/memory/StackFrameViz.tsx` | `from-slate-900...` + `bg-slate-950` | 2处 |
| PointerSandbox | `src/components/memory/PointerSandbox.tsx` | `bg-slate-950` | 3处 |
| PointerMemoryViz | `src/components/memory/PointerMemoryViz.tsx` | `from-slate-900...` + `bg-slate-950` | 2处 |

### 12.1.3 其他组件固定深色背景问题

| 组件 | 文件路径 | 问题代码 | 严重程度 |
|------|----------|----------|---------|
| CodeAnalyzer | `src/components/learning/CodeAnalyzer.tsx` | `bg-[#0d1117]`(3处)、`bg-[#161b22]`(4处)、`text-[#8b949e]`、`text-[#58a6ff]` | 高 |
| CodeTypingPractice | `src/components/CodeTypingPractice.tsx` | 大量 `bg-slate-800/900`、`text-slate-200/300/400`、`border-slate-600/700` | 高 |

### 12.1.4 App.tsx 中 OrderQuestion 组件问题

`src/App.tsx` 中的 OrderQuestion 组件使用了以下固定深色样式：
- `bg-slate-700/50` 用于撤销/重置按钮背景
- `text-slate-400` / `text-slate-600` 等深色模式文字颜色

### 12.1.5 双主题机制冲突问题（架构级）

项目中存在**两套主题切换机制**，可能导致浅色模式无法完全生效：

| 机制 | 使用位置 | 实现方式 |
|------|---------|---------|
| **机制 A** | `src/App.tsx` + `src/index.css` | `body.classList.add/remove('dark')` + `body:not(.dark)` CSS 选择器 |
| **机制 B** | `src/styles/tokens.css` | `:root[data-theme="dark"]` / `:root[data-theme="light"]` + CSS 变量 |

**问题**：如果主题切换时只设置了 `body.dark` 而没有设置 `:root[data-theme]`，则 `tokens.css` 中的浅色变量不会生效。反之亦然。

**解决建议**：LLM 实现时应在主题切换逻辑中**同时更新两处**：
```tsx
// 切换主题时
document.body.classList.toggle('dark', isDarkMode);
document.documentElement.setAttribute('data-theme', isDarkMode ? 'dark' : 'light');
```

## 12.2 修复规范

### 12.2.1 CSS 样式修复方案（`src/index.css`）

在 `src/index.css` 中为所有缺少适配的样式添加 `body:not(.dark)` 浅色模式规则：

#### (a) 排序题型修复

```css
/* 浅色模式适配 */
body:not(.dark) .order-result-area {
  border: 1px solid rgba(0, 0, 0, 0.08);
  background: rgba(0, 0, 0, 0.02);
}

body:not(.dark) .order-line-btn {
  background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
  border: 1px solid rgba(0, 0, 0, 0.1);
  color: #334155;
}

body:not(.dark) .order-line-btn:hover {
  border-color: rgba(245, 158, 11, 0.5);
  background: rgba(245, 158, 11, 0.05);
}
```

#### (b) 可视化组件修复（`.viz-*` 系列）

```css
/* 浅色模式适配 */
body:not(.dark) .viz-container {
  background: rgba(0, 0, 0, 0.02);
  border: 1px solid rgba(0, 0, 0, 0.06);
}

body:not(.dark) .viz-memory-cell,
body:not(.dark) .viz-array-cell,
body:not(.dark) .viz-ptr-box,
body:not(.dark) .viz-flow-action,
body:not(.dark) .viz-stack-frame {
  background: linear-gradient(135deg, #f8fafc, #f1f5f9);
  border-color: rgba(0, 0, 0, 0.08);
}

body:not(.dark) .viz-flow-action {
  color: #475569;
}

body:not(.dark) .viz-table td {
  border-bottom: 1px solid rgba(0, 0, 0, 0.06);
  color: #334155;
}

body:not(.dark) .viz-execution {
  background: rgba(0, 0, 0, 0.04);
}

body:not(.dark) .viz-exec-cell {
  background: rgba(0, 0, 0, 0.02);
  border: 1px solid rgba(0, 0, 0, 0.06);
}
```

#### (c) 滚动条修复

```css
body:not(.dark) ::-webkit-scrollbar-track {
  background: rgba(0, 0, 0, 0.03);
}

body:not(.dark) ::-webkit-scrollbar-thumb {
  background: rgba(0, 0, 0, 0.15);
}

body:not(.dark) ::-webkit-scrollbar-thumb:hover {
  background: rgba(0, 0, 0, 0.25);
}
```

#### (d) 其他样式修复

```css
body:not(.dark) .list-item-hover:hover {
  background: rgba(0, 0, 0, 0.03);
}

body:not(.dark) .btn-liquid::after {
  background: rgba(0, 0, 0, 0.08);
}

body:not(.dark) .nav-slider {
  box-shadow: 0 0 20px rgba(34, 211, 238, 0.08),
              inset 0 1px 0 rgba(255, 255, 255, 0.8);
}
```

### 12.2.2 组件背景修复方案

所有实验室组件必须接收 `isDarkMode` 属性，并根据该属性动态切换背景：

```tsx
// 修复前（固定深色）
<div className="bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 rounded-2xl p-6...">

// 修复后（支持主题切换）
<div className={`rounded-2xl p-6 border shadow-2xl max-w-md mx-auto ${
  isDarkMode 
    ? 'bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 border-purple-500/30' 
    : 'bg-gradient-to-br from-white via-purple-50 to-slate-100 border-purple-300 shadow-lg'
}`}>
```

### 12.2.3 OrderQuestion 组件修复

```tsx
// 修复前
<button
  onClick={handleUndo}
  disabled={selectedOrder.length === 0 || showResult}
  className="text-xs px-2 py-1 rounded bg-slate-700/50 text-slate-400 hover:text-white disabled:opacity-30 transition-all flex items-center gap-1"
>

// 修复后
<button
  onClick={handleUndo}
  disabled={selectedOrder.length === 0 || showResult}
  className={`text-xs px-2 py-1 rounded disabled:opacity-30 transition-all flex items-center gap-1 ${
    isDarkMode 
      ? 'bg-slate-700/50 text-slate-400 hover:text-white' 
      : 'bg-slate-200 text-slate-600 hover:text-slate-800 hover:bg-slate-300'
  }`}
>
```

### 12.2.4 CodeAnalyzer / CodeTypingPractice 修复

这两个组件包含大量硬编码深色值，需要全面替换为 `isDarkMode` 条件判断：

```tsx
// 修复前
<div className="bg-[#0d1117] border border-[#30363d] rounded-lg">
  <span className="text-[#8b949e]">变量名</span>
  <span className="text-[#58a6ff]">关键字</span>
</div>

// 修复后
<div className={`rounded-lg border ${
  isDarkMode 
    ? 'bg-[#0d1117] border-[#30363d]' 
    : 'bg-white border-slate-200'
}`}>
  <span className={isDarkMode ? 'text-[#8b949e]' : 'text-slate-500'}>变量名</span>
  <span className={isDarkMode ? 'text-[#58a6ff]' : 'text-blue-600'}>关键字</span>
</div>
```

## 12.3 主题切换实现原理

### 12.3.1 当前实现

主题切换通过以下机制实现：

1. **状态管理**: `App.tsx` 中使用 `useState` 管理 `isDarkMode` 状态
2. **localStorage 持久化**: 主题偏好保存到 `localStorage`，key 为 `'theme'`
3. **body 类名切换**: 通过 `document.body.classList.add/remove('dark')` 切换主题
4. **CSS 适配**: 使用 `body:not(.dark)` 选择器为浅色模式定义样式

### 12.3.2 传递机制

`isDarkMode` 通过 props 向下传递到各组件：

```
App (isDarkMode state)
  ├── Header (接收 isDarkMode)
  ├── HomeView (接收 isDarkMode)
  ├── TrainingView (接收 isDarkMode)
  │     └── QuestionView (接收 isDarkMode)
  │           └── OrderQuestion (接收 isDarkMode) ← 需要修复
  ├── LabView (接收 isDarkMode)
  │     ├── PointerSandbox ← 需要修复，需传递 isDarkMode
  │     ├── StackFrameViz ← 需要修复，需传递 isDarkMode
  │     ├── COWMemoryViz ← 需要修复，需传递 isDarkMode
  │     ├── BitSwitchGame ← 需要修复，需传递 isDarkMode
  │     └── StructPackerGame ← 需要修复，需传递 isDarkMode
  └── ...
```

## 12.4 修复检查清单

### CSS 修复（`src/index.css`）

- [ ] `.order-result-area` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.order-line-btn` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.viz-container` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.viz-memory-cell` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.viz-array-cell` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.viz-ptr-box` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.viz-flow-action` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.viz-stack-frame` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.viz-table td` - 添加 `body:not(.dark)` 浅色适配（修复文字颜色）
- [ ] `.viz-execution` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.viz-exec-cell` - 添加 `body:not(.dark)` 浅色适配
- [ ] `::-webkit-scrollbar-*` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.list-item-hover:hover` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.btn-liquid::after` - 添加 `body:not(.dark)` 浅色适配
- [ ] `.nav-slider` - 添加 `body:not(.dark)` 浅色适配

### 组件修复

- [ ] `BitSwitchGame.tsx` - 添加 `isDarkMode` 属性，修复背景渐变
- [ ] `StructPackerGame.tsx` - 添加 `isDarkMode` 属性，修复背景渐变
- [ ] `COWMemoryViz.tsx` - 添加 `isDarkMode` 属性，修复背景渐变
- [ ] `StackFrameViz.tsx` - 添加 `isDarkMode` 属性，修复背景渐变 + `bg-slate-950`
- [ ] `PointerSandbox.tsx` - 添加 `isDarkMode` 属性，修复 3 处 `bg-slate-950`
- [ ] `PointerMemoryViz.tsx` - 添加 `isDarkMode` 属性，修复背景渐变 + `bg-slate-950`
- [ ] `CodeAnalyzer.tsx` - 添加 `isDarkMode` 属性，修复所有 `bg-[#0d1117]` / `bg-[#161b22]` / `text-[#...]`
- [ ] `CodeTypingPractice.tsx` - 添加 `isDarkMode` 属性，修复大量硬编码深色 Tailwind 类
- [ ] `LabView` - 确保向所有子组件传递 `isDarkMode`

### App.tsx 修复

- [ ] `OrderQuestion` 组件 - 修复按钮和文字颜色

### 架构修复

- [ ] 主题切换逻辑中同时设置 `body.dark` 和 `:root[data-theme]`

## 12.5 最佳实践

### 12.5.1 新组件开发规范

开发新组件时，必须遵循以下规范以支持主题切换：

1. **接收 `isDarkMode` 属性**:
```tsx
interface MyComponentProps {
  isDarkMode: boolean;
}
```

2. **使用条件类名**:
```tsx
<div className={`p-4 rounded-lg ${
  isDarkMode 
    ? 'bg-slate-800 text-white border-slate-700' 
    : 'bg-white text-slate-800 border-slate-200'
}`}>
```

3. **避免使用固定深色值**:
- ❌ 避免: `bg-slate-900`, `bg-gray-900`, `bg-zinc-900` 等固定深色背景
- ❌ 避免: `text-slate-100`, `text-white` 等固定浅色文字（在深色模式下）
- ❌ 避免: `bg-[#0d1117]`, `bg-[#161b22]`, `text-[#e6edf3]` 等硬编码十六进制颜色
- ❌ 避免: `from-slate-900`, `via-slate-800` 等固定深色渐变
- ✅ 推荐: 使用 `isDarkMode ? '深色样式' : '浅色样式'` 条件判断

4. **CSS 类的浅色适配**:
编写 CSS 类时，必须同时提供深色和浅色模式样式：
```css
/* 深色模式（默认） */
.my-class {
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
  color: #e6edf3;
}

/* 浅色模式 - 必须添加 */
body:not(.dark) .my-class {
  background: rgba(0, 0, 0, 0.02);
  border: 1px solid rgba(0, 0, 0, 0.08);
  color: #334155;
}
```

5. **半透明颜色适配规则**:
- 深色模式: `rgba(255, 255, 255, 0.0x)` 用于背景微光效果
- 浅色模式: 对应改为 `rgba(0, 0, 0, 0.0x)` 用于背景阴影效果

### 12.5.2 颜色映射参考

#### Tailwind 类名映射

| 用途 | 深色模式 | 浅色模式 |
|------|----------|----------|
| 主背景 | `bg-slate-900` / `bg-gradient-to-br from-slate-900...` | `bg-white` / `bg-slate-50` / `bg-gradient-to-br from-white...` |
| 卡片背景 | `bg-slate-800/50` | `bg-white/80` |
| 边框 | `border-slate-700` | `border-slate-200` |
| 主文字 | `text-white` | `text-slate-800` |
| 次要文字 | `text-slate-400` | `text-slate-600` |
| 提示文字 | `text-slate-500` | `text-slate-400` |
| 代码块背景 | `bg-slate-950` | `bg-slate-100` |
| 高亮背景 | `bg-slate-800` | `bg-slate-100` |
| 悬浮背景 | `rgba(255,255,255,0.03)` | `rgba(0,0,0,0.03)` |

#### CSS 颜色值映射

| 用途 | 深色模式 | 浅色模式 |
|------|----------|----------|
| 代码背景 | `#0d1117` / `#161b22` | `#f8fafc` / `#f1f5f9` |
| 代码文字 | `#e6edf3` | `#334155` |
| 代码行号 | `rgba(255,255,255,0.4)` | `rgba(0,0,0,0.4)` |
| 单元格背景 | `#1e293b` / `#0f172a` | `#f8fafc` / `#f1f5f9` |
| 单元格边框 | `rgba(255,255,255,0.05)` | `rgba(0,0,0,0.06)` |
| 表格文字 | `#cbd5e1` | `#334155` |

### 12.5.3 界面架构原则（LLM 开发指南）

LLM 在实现此项目的界面时，应遵循以下架构原则：

1. **整体布局**：单页面应用（SPA），通过 `view` 状态字符串切换视图，不使用路由
2. **主题切换**：全局 `isDarkMode` 状态，通过 props 层层传递
3. **样式方案**：优先使用 Tailwind CSS 条件类名，全局样式使用 `index.css` + `body:not(.dark)` 适配
4. **组件接收规范**：每个可能需要主题切换的组件都必须在 props 接口中声明 `isDarkMode: boolean`
5. **新写 CSS 类时**：必须同时写深色和浅色两套样式，不能只写深色模式
6. **视觉一致性**：所有同类型的 UI 元素（按钮、卡片、代码块等）在深色和浅色模式下都应保持一致的视觉风格，只是配色不同

### 12.5.4 测试检查项

在提交代码前，请确保：
- [ ] 在深色模式下检查组件显示正常
- [ ] 在浅色模式下检查组件显示正常
- [ ] 切换主题时过渡平滑，无闪烁
- [ ] 所有文字在两种模式下都清晰可读（对比度检查）
- [ ] 边框和阴影在两种模式下都可见
- [ ] 半透明背景在两种模式下效果协调
- [ ] 代码块在浅色模式下文字可读
- [ ] 可视化组件（.viz-*）在浅色模式下正常显示


# 十三、文档与代码一致性审查（LLM 实现注意事项）

> **⚠️ 重要**：以下问题经过对源代码与本文档的深度交叉审查发现，LLM 在实现项目时必须注意这些不一致之处，以本文档为准（代码为参考实现，可能存在遗留问题）。

## 13.1 题型数量不一致

**文档标题**（第三章）写"7 种题型"，但实际代码和数据定义中有 **10 种题型**。

| 题型 | type 值 | 文档有描述 |
|------|---------|-----------|
| 代码填空 | `fill` / `multi-fill` | 是 (3.1) |
| 输出预测 | `output` | 是 (3.2) |
| Debug 找错 | `debug` | 是 (3.3) |
| 代码排序 | `order` | 是 (3.4) |
| 选择题 | `choice` / `multi-choice` | 是 (3.5) |
| 代码追踪 | `trace` | 是 (3.6) |
| 概念理解 | `concept` | 是 (3.7) |
| 代码补全 | `code-complete` | 是 (3.7) |
| 阅读理解 | `reading` | 是 (3.7) |

**LLM 实现时**：按 10 种题型实现，确保 QuestionView 中包含所有题型的渲染逻辑。

## 13.2 已废弃 / 不存在的文件

以下文件在文档中被描述但实际代码中**不存在或已废弃**：

| 文档描述的路径 | 文档位置 | 实际状态 | 处理建议 |
|---------------|---------|---------|---------|
| `src/services/aiTutorService.ts` | 第十章 | 不存在 | AI 辅导逻辑已内联到 `src/components/ai/AITutor.tsx` 中，LLM 无需创建此文件 |
| `src/styles/global.css` | - | 存在但**未被 import** | 死文件，实际未加载 |
| `src/styles/tokens.css` | - | 存在但**未被 import** | 死文件，实际未加载 |

## 13.3 重复组件文件问题

项目中存在 **19 个组件在根目录和子目录之间重复**，两份文件内容可能不一致：

| 组件 | 根目录路径（App.tsx 实际使用） | 子目录路径（未被引用） |
|------|---------------------------|---------------------|
| PointerSandbox | `src/components/PointerSandbox.tsx` | `src/components/memory/PointerSandbox.tsx` |
| PointerMemoryViz | `src/components/PointerMemoryViz.tsx` | `src/components/memory/PointerMemoryViz.tsx` |
| COWMemoryViz | `src/components/COWMemoryViz.tsx` | `src/components/memory/COWMemoryViz.tsx` |
| StackFrameViz | `src/components/StackFrameViz.tsx` | `src/components/memory/StackFrameViz.tsx` |
| BitSwitchGame | `src/components/BitSwitchGame.tsx` | `src/components/games/BitSwitchGame.tsx` |
| StructPackerGame | `src/components/StructPackerGame.tsx` | `src/components/games/StructPackerGame.tsx` |
| CodeAnalyzer | `src/components/CodeAnalyzer.tsx` | `src/components/learning/CodeAnalyzer.tsx` |
| DeepUnderstandingPanel | `src/components/DeepUnderstandingPanel.tsx` | `src/components/learning/DeepUnderstandingPanel.tsx` |
| ReferenceSidebar | `src/components/ReferenceSidebar.tsx` | `src/components/learning/ReferenceSidebar.tsx` |
| EnhancedVisualization | `src/components/EnhancedVisualization.tsx` | `src/components/learning/EnhancedVisualization.tsx` |
| ProjectView | `src/components/ProjectView.tsx` | `src/components/learning/ProjectView.tsx` |
| EnhancedMemoryViz | `src/components/EnhancedMemoryViz.tsx` (7.64KB) | `src/components/memory/EnhancedMemoryViz.tsx` (25.84KB) |
| 其他 7 个 | ThemeToggle, LiquidNav, LiquidTabs, PageTransition, StatsAndAchievements, DashboardLayout, DashboardExample | 对应 layout/ 子目录 |

**LLM 实现时**：只使用根目录路径（App.tsx import 的路径），忽略子目录中的副本。子目录文件为冗余副本，无需维护。

## 13.4 App.tsx 中的死导入（未被使用的组件）

以下组件被 import 但在渲染中从未使用，是死代码：

| 组件 | 导入路径 | 建议 |
|------|---------|------|
| DashboardLayout | `src/components/DashboardLayout` | 不实现，直接删除 |
| DashboardExample | `src/components/DashboardExample` | 不实现，直接删除 |
| EnhancedVisualization | `src/components/EnhancedVisualization` | 不实现，直接删除 |
| PointerMemoryViz | `src/components/PointerMemoryViz` | 不实现，直接删除 |

## 13.5 CSS 文件加载情况

**实际加载链**（`main.tsx`）：
```
main.tsx → import "./index.css"  （唯一被加载的 CSS 文件）
```

**未被加载的 CSS 文件**：
- `src/styles/global.css` — 未被任何文件 import，是死文件
- `src/styles/tokens.css` — 未被任何文件 import，是死文件

**因此**：第 12.1.5 节描述的"双主题机制冲突"问题**实际上不存在**——因为 `tokens.css` 根本没被加载，只有 `body.dark` 机制在生效。

**LLM 实现时**：
- 只需维护 `src/index.css` 中的 `body.dark` / `body:not(.dark)` 机制
- 无需处理 `data-theme` 属性
- 可以忽略 `src/styles/` 目录下的文件

## 13.6 文档未描述的重要组件

### 13.6.1 `src/components/ui/` 组件库

项目中存在一个 UI 基础组件库，文档完全未提及：

| 文件 | 说明 |
|------|------|
| `src/components/ui/Icon.tsx` | 统一图标系统（App.tsx 实际在使用） |
| `src/components/ui/Button.tsx` | 按钮组件 |
| `src/components/ui/Card.tsx` | 卡片组件 |
| `src/components/ui/LoadingStates.tsx` | 加载状态组件 |
| `src/components/ui/OptimizedUI.tsx` | 优化 UI 组件 |
| `src/components/ui/RippleButton.tsx` | 涟漪按钮 |
| `src/components/ui/Toast.tsx` | Toast 通知 |

**LLM 实现时**：如果使用 `ui/Icon.tsx`，需确保图标系统正确导出所有需要的图标。

### 13.6.2 Question 接口的完整字段

文档未完整列出 Question 接口的所有字段，以下为实际代码中存在但文档描述不完整的重要字段：

```typescript
interface Question {
  id: string;
  chapterId: string;
  type: 'fill' | 'output' | 'debug' | 'order' | 'multi-fill' | 'choice' | 'multi-choice' | 'trace' | 'concept' | 'code-complete' | 'reading';
  code?: string;                    // 题目代码
  question?: string;                // 题目描述
  answer?: string | string[];       // 答案（支持多个正确答案）
  hint?: string;                    // 提示
  explanation?: string;             // 解析
  difficulty?: 1 | 2 | 3;          // 难度
  caseSensitive?: boolean;          // 是否大小写敏感
  
  // debug 题专用
  errorLine?: number;
  bugFix?: string;
  
  // order 题专用
  shuffledLines?: string[];
  correctOrder?: number[];
  correctOutput?: string;
  
  // choice 题专用
  options?: string[];
  multiCorrect?: boolean;
  
  // trace 题专用
  traceQuestion?: string;
  correctTrace?: string;
  
  // concept / code-complete 题专用
  conceptText?: string;
  completionPrompt?: string;
  correctCode?: string;
  
  // reading 题专用
  readingPassage?: string;
  
  // 深度分析相关
  lineAnalysis?: CodeLineAnalysis[];    // 逐行代码分析
  memoryViz?: MemoryVisualization;       // 内存可视化数据
  knowledgePoints?: string[];            // 知识点标签
  commonMistakes?: string[];             // 常见错误
  relatedConcepts?: string[];            // 相关概念
  
  // 可视化
  visualization?: { type: 'html'; content: string };
}
```

## 13.7 视图数量说明

文档描述"8 个主要视图"，实际代码中有 9 个 `view` 状态值，其中 `projects`（项目列表）和 `project`（项目详情）被合并描述为"项目实战"。LLM 实现时按 9 个 view 值处理：

`home` | `training` | `stats` | `review` | `bookmarked` | `projects` | `project` | `lab` | `typing`

## 13.8 新题型 Badge 显示缺失

当前代码中 `renderTypeBadge` 函数只为 7 种基础题型定义了 Badge 样式，`trace`/`concept`/`code-complete`/`reading` 四种新题型走了 `default` 分支（显示为默认灰色"其他"标签）。

**LLM 实现时**：应为这 4 种新题型补充专属的 Badge 样式和图标，例如：
- trace → 蓝色标签 "代码追踪"
- concept → 紫色标签 "概念理解"
- code-complete → 青色标签 "代码补全"
- reading → 橙色标签 "阅读理解"

*— 文档完 —*
